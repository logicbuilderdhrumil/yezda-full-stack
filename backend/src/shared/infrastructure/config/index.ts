/**
 * Configuration (Shared Infrastructure)
 *
 * Re-exports from the canonical source for use by clean-architecture modules.
 * The canonical implementation lives in `src/config/index.ts`.
 */

export { config, type AuthConfig, type OAuthProviderEnv } from '../../../config/index.js';
export { default } from '../../../config/index.js';
