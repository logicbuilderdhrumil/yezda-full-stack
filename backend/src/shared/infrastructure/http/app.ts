/**
 * Express App (Shared Infrastructure)
 *
 * Re-exports the configured Express application for use by clean-architecture modules.
 * The canonical setup lives in `src/app.ts`.
 */

export { default } from '../../../app.js';
