# Change: Add UI component kit

## Why
Provide a consistent, reusable set of UI primitives for all pages.

## What Changes
- Build core UI components (buttons, inputs, dialogs, tables, etc.).
- Provide shared styling, sizing, and theming for components.
- Add hooks and utilities to support component behavior.

## Impact
- Affected specs: ui-kit
- Affected code: src/components/ui
